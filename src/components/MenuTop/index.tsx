import { Nav } from "../Nav"
import { ContainerWrapper } from "./styles"

export const MenuTop = () => {
    return(
        <ContainerWrapper>
            <Nav></Nav>
        </ContainerWrapper>
    )
}