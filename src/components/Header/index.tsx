import { Nav } from "../Nav"
import { ContainerWrapper } from "./styles"

export const Header = () => {
    return(
        <ContainerWrapper>
            <Nav></Nav>
        </ContainerWrapper>
    )
}